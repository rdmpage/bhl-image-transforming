
.DS_Store
myfiles.txt
/junk/

# Generated by the compare scripts (routing pipeline + threshold explorer).
# Source (*.py, run.sh) and the labelled input/ test set are tracked; the
# derived artifacts below are reproducible by re-running the scripts.
compare/out/
compare/work/
compare/seg/
compare/*.png
compare/*.html
